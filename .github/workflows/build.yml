name: build
on:
  - push

jobs:
  build:
    name: Build binaries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
      - run: make build-all
        env:
          IS_CI_BUILD: '1'
